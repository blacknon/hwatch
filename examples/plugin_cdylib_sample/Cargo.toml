[package]
name = "hwatch-plugin-cdylib-sample"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
hwatch-diffmode = { path = "../../crates/diffmode", version = "0.4.2" }
similar = { version = "2.5.0", features = ["text"] }

[workspace]
